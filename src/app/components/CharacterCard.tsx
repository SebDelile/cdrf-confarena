import { CharacterProfileType } from '@/constants';

type PropTypes = {
  characterProfile: CharacterProfileType;
};

export const CharacterCard = ({ characterProfile }: PropTypes) => {
  return <div>(Carte générée à venir)</div>;
};
